#include <stdio.h>

int main()
{

    int a = 4;
    // int b = 8.5; // not recommanded because 8.5 is not an integer

    float b = 8.5;
    char c = 'u';
    int d = 77;
    int e = 4 + 77;
    

    printf("the value of a is %d \n", a);
    printf("the value of b is %f \n", b);
    printf("the value of c is %c \n", c);
    printf("the value of a+d is %d \n", a + d);
    printf("the value of a+d is %d\n", e);

    return 0;
}


// try it by yourself ----> create  a program to add two number in C 
//(homework 1  is a file)

